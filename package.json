{
  "name": "webhook-hub",
  "version": "1.0.0",
  "description": "Multi-tenant webhook processing platform",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "db:migrate": "wrangler d1 execute webhook-hub-db --remote --file=src/db/schema.sql"
  },
  "keywords": [],
  "author": "Noah Pilkington",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "hono": "^4.12.12"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.14.6",
    "@cloudflare/workers-types": "^4.20260414.1",
    "typescript": "^6.0.2",
    "vitest": "^4.1.4",
    "wrangler": "^4.82.2"
  }
}
